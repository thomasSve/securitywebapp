{% extends 'base.twig' %}

{% block content %}

    <h2>Posts </h2>

    {% for post in posts %}
        <div class="post">
            <a href="/posts/{{ post.postId|e('html_attr') }}">{{ post.title|e('html_attr')}}</a> by  <a href="/user/{{ post.author|e('html_attr') }}"> {{ post.author|e('html_attr') }}</a>. Posted on {{ post.date|e('html_attr') }}
        </div>
    {% else %}
        There are no posts as of yet
    {% endfor %}

{% endblock %}
